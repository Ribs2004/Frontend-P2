import React from "react";

import { Button } from "@/components/ui/button"

const Home = () => {
  return (
    <div>
      <Button className="text-xs" size="sm">Hello, world!</Button>
    </div>
  );
};

export default Home;
